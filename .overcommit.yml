# Use this file to configure the Overcommit hooks you wish to use. This will
# extend the default configuration defined in:
# https://github.com/sds/overcommit/blob/master/config/default.yml
#
# At the topmost level of this YAML file is a key representing type of hook
# being run (e.g. pre-commit, commit-msg, etc.). Within each type you can
# customize each hook, such as whether to only run it on certain files (via
# `include`), whether to only display output if it fails (via `quiet`), etc.
#
# For a complete list of hooks, see:
# https://github.com/sds/overcommit/tree/master/lib/overcommit/hook
#
# For a complete list of options that you can use to customize hooks, see:
# https://github.com/sds/overcommit#configuration
#
# Uncomment the following lines to make the configuration take effect.

PreCommit:
  RuboCop:
    enabled: true
    on_warn: fail # Treat all warnings as failures

  TrailingWhitespace:
    enabled: true
    exclude:
      - '/db/**/*' # Ignore trailing whitespace in generated files


#PostCheckout:
#  ALL: # Special hook name that customizes all hooks of this type
#    quiet: true # Change all post-checkout hooks to only display output on failure
#
#  IndexTags:
#    enabled: true # Generate a tags file with `ctags` each time HEAD changes
PrePush:
  Brakeman:
    enabled: true
    command: ['bundle', 'exec', 'brakeman']
    on_warn: fail

  RakeTarget:
    enabled: true
    description: 'Run rake tasks'
    targets:
      - 'db:prepare'
      - 'spec'
    command: ['bundle', 'exec', 'rake']

# CommitMsg:
#   Signoff:
#     enabled: true
#     required_executable: 'gh'
#     description: "Require commit signoff"
#     require_clean_working_directory: false
#     command:
#       - bash
#       - -c
#       - |
#         if ! gh signoff --help >/dev/null 2>&1; then
#           echo "Missing GitHub CLI extension: gh-signoff"
#           echo "Install with: gh extension install basecamp/gh-signoff"
#           exit 1
#         fi
#         gh signoff
#     required_executable_missing_message: "Skipping signoff check because gh is not installed"